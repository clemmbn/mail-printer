[project]
name = "mail-printer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "clemmbn", email = "clem.mbn@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mail-printer = "mail_printer:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
