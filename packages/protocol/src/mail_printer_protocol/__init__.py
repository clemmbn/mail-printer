"""Shared code between the mail-printer server and the Pi agent.

Holds the WebSocket message types (`messages`) and the common logging setup
(`logs`). STDLIB ONLY: this package is installed on both machines and must
stay dependency-free.
"""
